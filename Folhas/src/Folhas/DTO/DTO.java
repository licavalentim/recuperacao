package Folhas.DTO;

public record DTO (String nome, String tipo, String formato, String cor, String tamanho, String textura){
	// O record já cria automaticamente os métodos getter: nome(), tipo(), formato(), cor(), tamanho(), textura()	}
}
